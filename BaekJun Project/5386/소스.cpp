#include <bits/stdc++.h>

using namespace std;

int s, k;

int main()
{
	int tc; cin >> tc;
	while (tc--)
	{
		cin >> s >> k;

		int sum = 0;
		if (k == 1)
			sum = (s % 2);
		else
		{
		}
	}
	return 0;
}